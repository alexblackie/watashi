{
  "name": "com.alexblackie.watashi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build-css && npm run build-images && npm run build-misc",
    "build-css": "esbuild static/_/site.css --loader:.woff2=file --bundle --minify --outdir=public/_",
    "build-images": "cp -vr static/images/ public/images/",
    "build-misc": "cp -vr static/favicon.ico public/"
  },
  "author": "Alex Blackie <alex@blackie.me>",
  "license": "AGPL-3.0",
  "dependencies": {
    "esbuild": "^0.17.11"
  }
}

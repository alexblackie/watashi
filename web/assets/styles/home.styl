@import "includes/_constants";

#siteHeader {
  border: none;
}

#siteContent {
  padding-top: 0;
}

.intro {
  margin-bottom: 2rem;
  padding: 3em 0;
  font-size: 1.15em;
  line-height: 1.75em;
  color: rgba(255,255,255, 0.8);
  background: #252525;
  border-bottom: 6px solid $colorHighlightAlt;

  .container {
    box-sizing: border-box;
    padding: 0 $paddingContentX;
  }

  p {
    margin: 0;
  }

  a {
    font-weight: bold;
    border-bottom-width: 2px;
  }

  .intro-lead {
    margin: 0 0 0.5em;
    font-weight: bold;
    font-size: 1.25em;
    color: white;
  }
}

.home-two-up {
  display: flex;
}

.home-two-up .column {
  box-sizing: border-box;

  h2 {
    margin-top: 0;
  }

  dl {
    margin-top: 0;
  }

  dt,
  dd {
    border-bottom: 1px solid $colorBorderLight;

    &:hover {
      background: #fafafa;
    }
  }

  dt {
    float: left;
    width: 25%;
    padding: 0.5em 0.25em;
    color: $colorTextLight;
  }

  dd {
    margin: 0;

    a {
      display: block;
      padding: 0.5em 0.25em;
      border: none;
    }
  }
}

.home-articles {
  width: 60%;
  padding-right: 2rem;

  header {
    overflow: hidden;
  }

  h2 {
    float: left;
  }
}

.home-contact {
  width: 40%;
}

.home-articles-rss {
  float: right;
  border: none;

  svg {
    width: 24px;
    height: 24px;
  }

  &:hover svg path {
    fill: #d33d5d;
  }
}

.article-list-pagination {
  overflow: auto;
  padding-top: 1em;
}
.article-list-pagination-next {
  float: right;
}
.article-list-pagination-previous {
  float: left;
}


/*******************************************************************************
 * Media Queries
 ******************************************************************************/

@media screen and (max-width: 880px) {
  .intro {
    font-size: 1em;
    padding: 2em;

    .container {
      padding: 0;
    }
  }

  .home-two-up {
    flex-direction: column;

    .column {
      width: auto;
      padding: 0 0.5em;

      dt,
      dd {
        padding: 0.5em 0;
      }
    }
  }
}
